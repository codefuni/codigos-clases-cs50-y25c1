// Calculadora en C
#include <cs50.h>
#include <stdio.h>
#include <math.h>

int main()
{
    float a = 3.3;
    printf("Con ceil: %.2f\n", ceil(a));
    printf("Con floor: %.2f\n", floor(a));
    printf("Con round: %.2f\n", round(a));
}
